#include <stdio.h>

int main( )
{	
//	int n;
//	printf("구구단 입력 :");
//	scanf("%d",&n);
//	for (int i= 9; i >= 2; i-=1)
//	{	
//		printf("%d * %d = %d \n",n,i,n * i);
//	}
	
	
//	int n;
//	scanf("%d",&n);
//	
//	for (int i = 1;i <= n;i++)
//	{
//		if(i%3==0)
//		{
//			printf("%d\n",i);
//		}
//	}


	int n,sum=0,count=0;
	scanf("%d",&n);
	
	for(int i = 1;i <= n;i++)
	{
		if(i%6==0)
		{	
			count = count + 1;
			sum = sum + i;
		}

	}
	printf("%d \n",count);
	printf("%d \n",sum);	

//	int n,sum = 0;
//	scanf("%d",&n);
//	
//	for(int i = 1;i <= n;i+=1)
//	{
//		sum = sum + i;
//		
//	}	
//	printf("%d ",sum);
}
